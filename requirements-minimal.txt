# Inference (restore.py) needs only these three.
# CPU-only install, avoids ~4 GB of CUDA wheels:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0,<3.0.0
numpy>=1.24.0
Pillow>=9.0.0

# --- training / evaluation only; not needed by restore.py ---
# lpips>=0.1.4          # evaluate.py --lpips
# tifffile>=2023.1.0    # TIFF input
# matplotlib>=3.6.0     # notebook figures
# scikit-image>=0.20.0  # notebook only
# timm>=0.9.0           # SwinIR comparison
# einops>=0.6.0         # Restormer comparison
