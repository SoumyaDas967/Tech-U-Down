# Dependencies for run.py, with versions.
#
# On Linux the default PyPI torch wheel is the CUDA build, so this installs a
# GPU-capable PyTorch with no extra index or flag. It falls back to CPU
# automatically if no NVIDIA device is present.
#
# Install with:   pip install -r requirements.txt

torch==2.13.0
numpy==2.5.2
